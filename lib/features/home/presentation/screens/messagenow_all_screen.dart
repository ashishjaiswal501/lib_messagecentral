import 'package:flutter/material.dart';

class DashBoardScreen extends StatelessWidget {
  const DashBoardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      ' Dashboard Screen for Meesage now',
      style: TextStyle(fontSize: 24),
    ));
  }
}

class SMSScreen extends StatelessWidget {
  const SMSScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      ' SMS Screen for Meesage now',
      style: TextStyle(fontSize: 24),
    ));
  }
}



class ReportsScreen extends StatelessWidget {
  const ReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      ' Reports Screen for Meesage now',
      style: TextStyle(fontSize: 24),
    ));
  }
}


class ApplicationBarScreen extends StatelessWidget {
  const ApplicationBarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      ' Application Bar Screen for Meesage now',
      style: TextStyle(fontSize: 24),
    ));
  }
}

class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      ' Settings Screen for Meesage now',
      style: TextStyle(fontSize: 24),
    ));
  }
}

